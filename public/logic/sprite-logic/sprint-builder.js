class BuildSprite {
    constructor(who, what, when, where, how) {
        this.who = who;
        this.what = what;
        this.when = when;
        this.where = where;
        this.how = how;
    };

    getLoc () {
        // let loc = new SpritePlace();
    };
};